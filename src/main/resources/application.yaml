spring:
  application:
    name: jarFactory

  datasource:
    url: jdbc:postgresql://localhost:5432/jar_factory
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect